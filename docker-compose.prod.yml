# Legacy override for docker-compose.yml. It is not the production installation target.
# This file is superseded by compose.yml and is kept for backward compatibility only.
services:
  api:
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
  worker-pool:
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
  redis:
    restart: unless-stopped
  tui:
    restart: unless-stopped
