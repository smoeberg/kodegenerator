# Legacy override for docker-compose.yml. It is not the DOR demo contract.
services:
  api:
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
  worker-pool:
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
  redis:
    restart: unless-stopped
  tui:
    restart: unless-stopped
