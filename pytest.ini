[pytest]
asyncio_mode = auto
# Only collect the canonical suite under tests/. Phase5 slice-local tests
# perform sys.path inserts that shadow the top-level ``execution`` package
# (phase5/.../execution.py), which breaks pipeline executor imports.
testpaths = tests
markers =
    acceptance: black-box tests that start the real HTTP service
