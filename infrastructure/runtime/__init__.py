"""Phase 7 production runtime primitives.

The runtime package contains infrastructure-only adapters. Domain and API code
should depend on small protocols rather than concrete PostgreSQL/S3 clients.
"""
