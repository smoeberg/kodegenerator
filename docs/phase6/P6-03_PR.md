P6-03 adds the first concrete process isolation backend using bubblewrap. It fails closed when unavailable, uses Linux namespaces, read-only host filesystem semantics, explicit writable paths, executable allowlisting, resource limits, timeout, and bounded output. Network is deny-only until P6-05.